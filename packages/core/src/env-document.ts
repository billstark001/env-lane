// Stable feature entry point. The implementation remains behind this facade so its internal
// layer can move without changing `@env-lane/core/env-document`.
export * from './application/env-document.js'
